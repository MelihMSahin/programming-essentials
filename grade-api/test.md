| Test Scenario                                 | Request                          | Data Entered to Test                       | Expected Output                                                                            | Actual Output |
| --------------------------------------------- | -------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------ | ------------- |
| Retrieve all students                         | GET `/api/Students`              | None                                       | HTTP 200 OK. Returns a JSON array of students with `id`, `name`, and `score` fields.       |       Expected Outcome        |
| Create a valid student                        | POST `/api/Students`             | `{ "name": "John Smith", "score": 85 }`    | HTTP 201 OK. Returns created student object including generated `id`, `name`, and `score`. |       Expected Outcome        |
| Create student with maximum valid score       | POST `/api/Students`             | `{ "name": "Jane Doe", "score": 100 }`     | HTTP 201 OK. Student created successfully.                                                 |       Expected Outcome        |
| Create student with minimum valid score       | POST `/api/Students`             | `{ "name": "Jane Doe", "score": 0 }`       | HTTP 200 OK. Student created successfully.                                                 |       Excpected Outocme        |
| Create student with score above allowed range | POST `/api/Students`             | `{ "name": "Test Student", "score": 110 }` | HTTP 400 Bad Request. Validation error: "The field Score must be between 0 and 100."       |       Expected Outcome        |
| Create student with negative score            | POST `/api/Students`             | `{ "name": "Test Student", "score": -1 }`  | HTTP 400 Bad Request. Validation error for score range.                                    |       Expected Outcome         |
| Retrieve student by valid ID                  | GET `/api/Students/{id}`         | Existing student ID                        | HTTP 200 OK. Returns matching student record.                                              |        Expected Outcome        |
| Retrieve student with invalid/non-existent ID | GET `/api/Students/{id}`         | Non-existent ID                            | 404 Not Found)                                         |       Expected Outcome         |
| Update student details                        | PUT `/api/Students/{id}`         | `{ "name": "Updated Name", "score": 90 }`  | HTTP 204 OK. Student details updated successfully.                                         |      Expected Outcome          |
| Update student with invalid score             | PUT `/api/Students/{id}`         | `{ "name": "Updated Name", "score": 150 }` | HTTP 400 Bad Request. Validation error for score range.                                    |       Expected Outcome        |
| Delete existing student                       | DELETE `/api/Students/{id}`      | Existing student ID                        | HTTP 200 OK. Student removed successfully.                                                 |       HTTP 204. Student removed.        |
| Delete non-existent student                   | DELETE `/api/Students/{id}`      | Non-existent ID                            | 404 Not Found.                                         |       Expected Outcome         |                                 |               |
